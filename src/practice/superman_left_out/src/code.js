import {introduce, newLine} from '../../../modules/introduce.mjs';

const heroes = ["Superman", "Thor", "Batman", "La Mujer Maravilla"];

for(let i = 0; i < heroes.length; i++) {
  alert(heroes[i]);
}